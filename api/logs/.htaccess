# Prevent direct access to log files
<Files "*.log">
    <IfModule authz_core_module>
        Require all denied
    </IfModule>
    <IfModule !authz_core_module>
        Deny from all
    </IfModule>
</Files>

# Disable directory browsing
Options -Indexes
